"""Application migration package.

The existing ``app.py`` remains the executable application while persistence
is moved behind repositories. This package contains the new configuration,
CouchDB, domain, and migration boundaries.
"""
